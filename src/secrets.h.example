// Copy this file to `src/secrets.h` and fill in your real values.
// `secrets.h` is gitignored — never commit your WiFi password.
#pragma once

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
